apiVersion: v1
kind: PersistentVolume
metadata:
  name: weather-pv
  # namespace removed: PVs are cluster-scoped
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: weather-storage
  hostPath:
    path: /data/weather
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: weather-pvc
  namespace: weather-monitoring
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: weather-storage
  resources:
    requests:
      storage: 5Gi
